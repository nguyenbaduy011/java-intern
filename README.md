# Spring Boot Exception Demo

Gồm 2 folder:

- demoExceptionHandler: Demo xử lý exception theo cách @ExceptionHandler và @ControllerAdvice.
- demoValidation: Demo cách sử dụng `@Valid` và `@Validated` trong Spring Boot.
